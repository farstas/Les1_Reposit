Сервак:
ec2-52-27-212-140.us-west-2.compute.amazonaws.com

int() # превращает в целое число
float() # превращает в вещественное число
bool() # превращает в логическое значение
str() # превращает в строку

import math # загрузка математических функций (синусы, логарифмы и прочее)

round(a, 2) # округление переменной а до 2 знаков после запятой
math.floor(a) # округление переменной а до целого вниз
math.ceil(a) # округление переменной а до целого вверх

a.upper() # переводит все символы переменной а в заглавные символы
a.lower() # переводит все символы переменной а в прописные символы
a.capitalize() # приводит слово к формату: первая буква заглавная, остальные прописью
a.trim() # удаляет пробелы и другие служебные символы из начала и с конца строки
a.replace('И', 'а') # заменяет в переменной а все символы 'И' на символ 'а'

a.split('.') # разбивает строку a на список, разделяя элементы по символу, указанному в скобках

symbol.isdigit() # проверяет значение символа и если это целое число, то выводит значение True, иначе False

Списки:
a[0] # возвращает первый элемент списка
a[2:4] # возырвщает список, состоящие из элементов списка а с третьего по четвертый (последний выдает минус 1)
a[-1] # выводит последний элемент списка
a.append(99) # добавляет элемент 99 в конец списка а
reversed(list) # переворачивает список list, первый элемент становится последним и наоборот
sorted(list) # сортирует список list
len(list) # считает количество элементов в списке или словаре
list.count('Z') # считает сколько раз повторился элемент Z в списке list
list.index('Z') # выводит индекс первого совпадения в списке
list.remove('Z') # удаляет элемент Z из списка

for key, value in catalog_item.item()
     print('{} : {}'.format(key, value)) # метод item выводит также значение ключа

del catalog_item['price'] # удаление ключа 'price' из словаря catalog_item
catalog_item.get('Z', 'Y') # метод для вывода значения ключа, чтобы не вызывать ошибку отсутствия ключа. Значение "Y" можно добавить, чтобы выводить при отсутствии ключа в словаре

Сервер:
ps afx # идентификатор процесса
kill номер процесса # остановка процесса с соответствующим номером
nohup python3 bot.py >/dev/null &  # запуск на сервере программы bot.py в автономном режиме

